[package]
name = "rco-entropy"
description = "RCO Protocol — Genesis entropy generation (hardware RNG + jitter whitening)"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[features]
default = ["std"]
std = ["rco-types/std", "getrandom"]

[dependencies]
rco-types = { workspace = true }
sha3 = { workspace = true }
getrandom = { version = "0.3", optional = true }

[dev-dependencies]
proptest = { workspace = true }
