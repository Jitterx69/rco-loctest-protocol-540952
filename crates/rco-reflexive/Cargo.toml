[package]
name = "rco-reflexive"
description = "RCO Protocol — Reflexive Jacobian & Manifold Stability"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
rco-types = { workspace = true }
rco-consensus = { workspace = true }
nalgebra = "0.32"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "jacobian_convergence"
harness = false
